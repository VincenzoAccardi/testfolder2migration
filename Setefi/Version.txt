3.7.1.7 GE
	- Creation
3.7.1.8 GE
	- StdBugFix : Don't set the bDelete flag to true even if an error occurs voiding a redeemed voucher.
3.7.1.9 GE
	- Changed from Private to Protected : m_theModCntr, m_taobj, m_colObjects, SetLastPaymentTransactionAmount, SetLastPaymentTransactionNumber
3.7.1.10 GE
	- BugFix : change CONTROLLO_CAMPI_CARTA from 1 to 2 in order to check the last 3 chars of the PAN instead of 4 chars.
	- BugFix : Don't print barcode on the copy of the eft receipt
3.7.1.11 GE
	- Improvement : Added code to debug received messages
3.7.1.12 GE
	- BugFix : Print the entire receipt
	- BugFix : Remove wrong characters from receipt (eg. ETX, CRC,...) 
3.7.1.13 GE
	- BugFix : Remove the character 0x1B from transaction
3.7.1.14 GE
	- Change : assembly name changed from TPDotnet.IT.Common.Pos.Setefi to TPDotnet.IT.Common.Pos.EFT.Setefi
	- Change : namespace changed from TPDotnet.IT.Common.Pos to TPDotnet.IT.Common.Pos.EFT
3.7.1.15 GE
	- BugFix : disabled the Annulla button (it seems to cause the double charge amount in particular situation)
3.7.1.16 GE
	- Change : Debug Tx messages
3.7.1.17 GE
	- BugFix : send the lWorkstationNmbr set to 0
3.7.1.18 GE
	- Change : Button Annulla disabled receiving the transaction result
	- Change : Test the transaction result returned by the EFT
	- Change : If the receipt is not received but the transaction is executed, show a warning and print a dummy receipt
	- Bugfix : Format the foreign currency as 42 chars for line instead of 21
	- Bugfix : Insert a blank line before the signature line
37.19.1.19 GE 20111005
	- Change : new version signature
	- Removed : function DoSpecialHandling4CreditCardsOffline (moved from NF_IT_Media project)
	- Removed : function VoidRedeemedVoucher (moved from NF_IT_Media project)
	- Removed : function DoSpecialHandling (it called only the base method)
40.26.0.0 GE 20120213
	- Porting to 4.0.2.6
	- Bugfix : Transmit an ACK after receiving the amount request (RX_START_TRANSACTION_REQUEST) from the EFT. The old specification is wrong.
	- Bugfix : function EFTResultHandler : the signature line (composed by "." chars) in not printed out. The presentation handling does not allow a line completely composed by ".", so lets replace it with "_"
40.26.0.1 GE 20121001
	- Add : handling for Close operation
40.26.0.2 GE 20121009
	- Add : EFT close operation must print the receipt
40.26.0.3 GE 20121011
	- Add : call EFT close only on LogOff
40.26.0.4 GE 20121115
	- BugFix : ACK sent twice for each EFT receipt message (this produce an incomplete EFT receipt)
	- Add : Log message in order to understand the problem of incomplete EFT receipt.
40.26.0.5 GE 20130121
	- Bugfix : thePosEFT.EFTHandler - sometimes the EFT resturns with a null/empty value within the field ESITO_TRANSAZIONE; manage this with an user request.
40.26.0.6 GE 20130121
	- Bugfix : thePosEFT.EFTResultHandler : set the right MyEftInfo.theHdr.lTaCreateNmbr
	- Bugfix : thePosEFT.EFTResultHandler : set the right MyEftInfo.theHdr.lTaSeqNmbr
40.26.0.7 GE 20130121
	- Bugfix : Setefi.ReadFromEFT : use ts.TotalSeconds instead of ts.Seconds because this cause problem with timeout values >= 60 seconds
5.00.0.0 GE AS 20140227
	- Porting 5.00.0.0 e framework.net 4
	- Eft.xml : added new value to element Protocol="Protocol21", where PaymentMessageToUse=4 is set as default
